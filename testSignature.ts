import { isReady, Signature, PublicKey, Field } from "snarkyjs";

async function main() {
  await isReady;

  const google_id = Field(''); // Fill the google id here
  const public_key = PublicKey.fromBase58(''); // Fill the public key from response
  const signature = {
    
  };
}

main();